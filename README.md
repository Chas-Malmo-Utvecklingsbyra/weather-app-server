# Weather App Server

Backend server for the Weather App.

## Prerequisites

Make sure you have the following installed:

- `curl`
- `gcc`

## How to Build & Run

```bash
git clone --recurse-submodules https://github.com/Chas-Malmo-Utvecklingsbyra/weather-app-server

cd weather-app-server

make run
